import ClassRoom from "./0-classroom"

export dconst initializeRooms = () => {
  const classrooms = new ClassRoom();
  const rooms = [
    new classrooms._maxStudentsSize = 19,
    new classrooms._maxStudentsSize = 20,
    new classrooms._maxStudentsSize = 34,
  ]

  return rooms;
}