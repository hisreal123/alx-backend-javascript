import ClassRoom from "./0-classroom"

export default function initializeRooms() {
  const classroom = new ClassRoom();
  const rooms = [
    classrooms._maxStudentsSize = 19,
    classrooms._maxStudentsSize = 20,
    classrooms._maxStudentsSize = 34,
  ]

  return rooms;
}