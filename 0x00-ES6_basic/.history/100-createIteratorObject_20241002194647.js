export default function createIteratorObject(report) {
  const reportWithIterator = () => {
    for (const vlaue in report) {
        return key;
        // if (Object.prototype.hasOwnProperty.call(object, key)) {
        //     const element = object[key];
            
        // }
    }
  }

  return reportWithIterator;
}
