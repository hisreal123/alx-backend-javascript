export default function iterateThroughObject(reportWithIterator) {
  const newObject = [];
  for (const employees of reportWithIterator) {
    newObject.pushemployees;
  }
  return newObject.join(' | ');
}
