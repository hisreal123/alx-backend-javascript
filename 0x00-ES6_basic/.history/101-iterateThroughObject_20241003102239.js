export default function iterateThroughObject(reportWithIterator) {
  const newObject = [];
  for (const [department, employees] of reportWithIterator) {
    newObject[depart];
  }
  return newObject;
}
