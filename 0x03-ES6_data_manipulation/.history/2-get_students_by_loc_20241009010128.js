/* eslint-disable no-unused-vars */
/* eslint-disable */

export default function getStudentsByLocation(array, location) {
  const _studentsByLocation = [];
  array.filter((student) => student.location === location)
}