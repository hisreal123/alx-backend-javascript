/* eslint-disable no-unused-vars */
/* eslint-disable */

const getStudentIdsSum = (listOfStudents) => {
  const _sumOfAllIds = [];
  return listOfStudents.reduce((sum, student) => sum +s)
}

export default getStudentIdsSum;
