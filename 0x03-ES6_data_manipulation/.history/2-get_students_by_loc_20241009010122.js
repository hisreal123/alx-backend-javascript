/* eslint-disable no-unused-vars */
/* eslint-disable */

export default function getStudentsByLocation(array) {
  const _studentsByLocation = [];
  array.filter((student) => student.location === loca)
}