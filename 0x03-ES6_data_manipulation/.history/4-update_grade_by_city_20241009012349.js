/* eslint-disable no-unused-vars */
/* eslint-disable */

const updateStudentGradeByCity = (studentss, city, newGrades) => {
  return array.filter((student) => student.city === city)

  return filteredArray;
};

export default updateStudentGradeByCity;
