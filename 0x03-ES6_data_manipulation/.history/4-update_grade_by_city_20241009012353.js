/* eslint-disable no-unused-vars */
/* eslint-disable */

const updateStudentGradeByCity = (students, city, newGrades) => {
  return a.filter((student) => student.city === city)

  return filteredArray;
};

export default updateStudentGradeByCity;
