/* eslint-disable no-unused-vars */
/* eslint-disable */

const getStudentsByLocation = (array, location) => array.filter((student) => student.location === location)

export default getStudentsByLocation;
