/* eslint-disable no-unused-vars */
/* eslint-disable */

const cleanset = (set, startString) => {
  const _parts = [];

  for (const elem of set) {
    if (typeof  elem === 'Srintelem.startsWith(startString)) {
  }
};

export default cleanset;
