/* eslint-disable no-unused-vars */
/* eslint-disable */

const getStudentIdsSum = (listOfStudents) =>  listOfStudents.reduce((sum, student) => sun + student.id ,);
}

export default getStudentIdsSum;
